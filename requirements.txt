# ── Departmental Store — Python Dependencies ──
# All features use Python's standard library only.
# Tkinter, datetime, os — all built-in, no pip install needed.

# Optional future enhancements (uncomment to enable):
reportlab==4.1.0       # Export bill as PDF
Pillow==10.3.0         # Add store logo/images to UI
openpyxl==3.1.2        # Export sales data to Excel